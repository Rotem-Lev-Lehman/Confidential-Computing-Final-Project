# Measurements: from-scratch Yao's Garbled Circuits

Generated by `experiments/run_experiments.py` on 2026-08-02 02:05:46 (full mode).

Every timed measurement below is a **genuine two-process 2PC run**: party 0 and party 1 are separate OS processes, each holding only its own share file, talking over real sockets. Wall time is what a user actually waits, including process spawn and rendezvous. `ot-2048` is the default configuration; `ot-1024` is the faster, weaker OT group (`--ot-group 1024`).

## 1. Environment

|  |  |
|---|---|
| Platform | Linux-5.14.0-611.47.1.el9_7.x86_64-x86_64-with-glibc2.34 |
| Python | 3.10.19 |
| CPU count | 12 |

## 2. Correctness (from the distributed runs)

| problem | regions | bits | ot-2048 = solution | ot-1024 = solution |
|---|---|---|---|---|
| problems/1 | 4 | 16 | ✓ | ✓ |
| problems/2 | 3 | 16 | ✓ | ✓ |
| problems/3 | 3 | 16 | ✓ | ✓ |
| problems/4 | 3 | 16 | ✓ | ✓ |
| problems/5 | 4 | 16 | ✓ | ✓ |
| problems/6 | 3 | 20 | ✓ | ✓ |
| problems/7 | 1 | 16 | ✓ | ✓ |
| problems/8 | 1 | 16 | ✓ | ✓ |
| problems/9 | 2 | 8 | ✓ | ✓ |
| problems/10 | 8 | 16 | ✓ | ✓ |

**Every configuration reveals exactly the expected output on every problem.**

## 3. Speed (two-process wall time)

### 3.1 Per problem

| problem | regions | bits | ot-2048 | ot-1024 |
|---|---|---|---|---|
| problems/1 | 4 | 16 | 7.67 s | 1.32 s |
| problems/2 | 3 | 16 | 5.66 s | 1.10 s |
| problems/3 | 3 | 16 | 5.70 s | 1.01 s |
| problems/4 | 3 | 16 | 5.67 s | 1.00 s |
| problems/5 | 4 | 16 | 7.56 s | 1.31 s |
| problems/6 | 3 | 20 | 7.03 s | 1.22 s |
| problems/7 | 1 | 16 | 1.93 s | 0.36 s |
| problems/8 | 1 | 16 | 1.93 s | 0.36 s |
| problems/9 | 2 | 8 | 2.03 s | 0.40 s |
| problems/10 | 8 | 16 | 15.09 s | 2.60 s |

### 3.2 Scaling with number of regions (16-bit shares)

| regions | ot-2048 | ot-1024 |
|---|---|---|
| 1 | 1.93 s | 0.36 s |
| 2 | 3.80 s | 0.68 s |
| 4 | 7.55 s | 1.32 s |
| 8 | 15.04 s | 2.60 s |

### 3.3 Scaling with share bit-width (4 regions)

| bit_length | ot-2048 | ot-1024 |
|---|---|---|
| 8 | 3.95 s | 0.77 s |
| 16 | 7.55 s | 1.32 s |
| 24 | 11.16 s | 1.87 s |
| 32 | 14.80 s | 2.42 s |

Cost grows linearly in `regions × bit_length`: the engine runs textbook **base OT**, one modular exponentiation per evaluator input bit. This is the dominant cost and the reason the deployed system keeps shares as narrow as the field allows (20 bits for `p = 2^20 - 3`). OT extension would amortize the base OTs and is the standard next step.

### 3.4 Fixed protocol overhead

| configuration | smallest possible run |
|---|---|
| ot-2048 | 1.02 s |
| ot-1024 | 0.22 s |

_1 region, 8-bit shares, two processes._ Subtracting this floor from the numbers above gives the marginal cost of the cryptography itself.

## 4. Communication cost

| configuration | garbler→evaluator B | evaluator→garbler B | total B | B/region | messages |
|---|---|---|---|---|---|
| ot-2048 | 85,949 | 39,941 | 125,890 | 31,472 | 20 |
| ot-1024 | 84,714 | 20,221 | 104,935 | 26,234 | 20 |

Measured exactly on the channel (problems/1, 4 regions × 16 bits). The garbled tables dominate: four 128-bit ciphertexts per AND gate. Traffic is a function of the public parameters only, so it leaks nothing about the inputs.

## 5. Implementation footprint

| component | files | code lines |
|---|---|---|
| `smpc_gc/yao/` — circuit, garbling, OT, protocol | 5 | 725 |
| `smpc_gc/threshold.py` — per-region driver | 1 | 158 |
| shared infrastructure (channel, types, mock, CLI) | 5 | 567 |
| transport + secure summation | 11 | 1478 |

**Runtime dependencies:** `cryptography`, used only for the standard primitives around the protocol (Ed25519, X25519, HKDF, AES-GCM). The SMPC itself — garbled circuits and oblivious transfer — is implemented from scratch on `hashlib`, `secrets` and `socket`.

## 6. Why there is no second engine

An earlier iteration put the evaluation behind a pluggable backend interface and added [MPyC](https://github.com/lschoe/mpyc) as a second engine, to compare against an established framework. It was removed. Two reasons, in order of importance:

1. **MPyC does not implement Yao's Garbled Circuits.** It is *honest-majority Shamir secret sharing* over arithmetic circuits — a different primitive. Benchmarking it against this engine compares two unrelated techniques, which is not the comparison the project is about.

2. **In the two-party setting it provides no input privacy at all.** Honest-majority Shamir requires `t < m/2`. With `m = 2` parties MPyC runs at threshold `t = 0`, and a degree-0 sharing polynomial is the constant `f(X) = secret` — so the "share" *is* the secret. We confirmed this experimentally before dropping it: party 1 read party 0's private input verbatim from its own share. Yao's protocol is what the proposal specifies precisely because it is secure for two parties.

MPyC would be a sound choice for a *different* architecture — one 4-party session across all four hospitals, skipping the reduction to two parties entirely, where `t = 1` and no single hospital learns anything. That is a real alternative design, and it is noted as such; it is not a drop-in backend for the proposal's hybrid pipeline.

With one engine, the `ThresholdBackend` abstraction and its registry were pure indirection, so they were removed too. `smpc_gc.threshold.evaluate_threshold` is now called directly.

